import React from "react";

const Home = (props) => (
            <div>
                home
                <button onClick={props.startQuiz}>Start!</button>
            </div>
)

export default Home;