import React from "react";

const Home = (props) => (
            <div>
                <button className="start-btn" onClick={props.startQuiz}>Start!</button>
            </div>
)

export default Home;